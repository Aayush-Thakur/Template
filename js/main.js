define(function(require) {
    var header = require("./header");               //All header animations and onclicks
    var carousel = require("./carousel");           //Carousel
    var skillsets = require("./skillSets");         //Animation for Skills bar
});